{{ $ns := .Release.Namespace }}
apiVersion: spacecontroller.infoblox-cto.github.com/v1alpha1
kind: Space
metadata:
  name: {{ .Values.tls.nginx.secretName }}
  namespace: {{ $ns }}
spec:
  path: {{ tpl .Values.tls.nginx.path . | quote }}
  secretName: {{ .Values.tls.nginx.secretName }}
  secrets:
    csp: "VAULT"
  type: {{ .Values.tls.nginx.type }}
---
